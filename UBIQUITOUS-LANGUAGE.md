# Ubiquitous Language

この文書は、人間とCodexが共有する、現在合意されているドメインモデルと言語を記述する。

提案中、未決定、または根拠が確認できない内容を、合意済みの事実として記載しない。

## Scope and Context

この文書は、デジタル庁デザインシステムの承認済みMarkdownを使うSkillと、その更新ライフサイクルを対象とする。

個別のWebサービスが採用する色、書体、余白などの値は、そのサービス側の文脈で決めるため対象外とする。

## Concepts and Canonical Terms

- **公式ソース**：更新契約に登録されたデジタル庁のURLから取得する情報。
- **有効スナップショット**：通常作業の根拠として現在承認されている、一つの公式Markdown集合。
- **派生資料**：公式Markdownから作る参照索引、基本デザイン対応表、検索用索引。
- **実行状態**：利用者ごとの確認成功日時、取得検証情報、保留中の候補IDを保持する情報。
- **更新期限到達**：前回の確認成功日時から30日以上経過した状態。
- **外部確認への同意**：公式ソースへの接続を許可する意思表示。
- **手動取得待ち**：公式ZIPのURLを特定した後に自動取得だけが失敗し、利用者による手動取得を待つ状態。
- **手動取得**：手動取得待ちで示された公式URLから、利用者がZIPを任意のローカル領域へ保存する操作。
- **ローカル取込**：手動取得したZIPをネットワークへ接続せずに検証し、変更があれば更新候補として保存する操作。
- **更新候補**：外部確認で見つけ、検証用に保存した未承認のスナップショット。
- **採用承認**：候補IDを指定し、有効スナップショットへの昇格を許可する意思表示。

## Model Grammar and Relations

- Skill = 1有効スナップショット AND 1..*派生資料 AND 1..*実行手順
- 利用者 = 0..1実行状態 AND 0..*更新候補
- 一つの実行状態は、0..1手動取得待ちを持つ。
- 一つの更新候補は、一つの有効スナップショットを基準版として持つ。

## Behaviors and State Transitions

- 実行状態がない場合、通常作業を止めず、完了後に初回の外部確認を提案する。
- 更新期限へ到達した場合、通常作業を先に完了し、状態を再確認してから外部確認を提案する。
- 外部確認への同意がある場合だけ、公式ソースへ接続する。
- 外部確認がすべて成功して変更がなければ、確認成功日時を更新し、有効スナップショットを維持する。
- 外部確認がすべて成功して変更があれば、更新候補を保存して確認成功日時を更新し、有効スナップショットを維持する。
- 公式ZIPのURLを特定した後に自動取得が失敗した場合、手動取得待ちへ移り、確認成功日時と有効スナップショットを維持する。
- 公式ZIPのURLを特定する前に失敗した場合、手動取得待ちへ移らず、URLを推測しない。
- 手動取得待ちが有効期間内であり、更新契約も一致する場合だけ、ローカル取込を実行できる。
- ローカル取込で変更がなければ取込完了を記録し、変更があれば更新候補を保存する。どちらも外部確認の成功には数えない。
- ローカル取込が成功した場合は手動取得待ちを解消し、有効スナップショットを維持する。
- 取得、保存、検証のいずれかが失敗した場合、確認成功日時と有効スナップショットを変更しない。
- 採用承認がある場合だけ、指定された更新候補を検証し、有効スナップショットへ昇格する。

## Invariants and Context Distinctions

- 通常作業は、有効スナップショットだけを公式上の根拠として使う。
- 更新期限への到達は外部通信を許可しない。
- 外部確認への同意は採用承認を意味しない。
- 手動取得待ちの有効期間は、公式ZIPのURLを特定してから30日である。
- 手動取得待ちの有効期間が過ぎた場合、または更新契約が変わった場合、ローカル取込を行わない。
- ローカル取込は新しい外部通信を行わず、手動取得元のZIPを変更または削除しない。
- 手動取得とローカル取込は採用承認を意味しない。
- ローカル取込は確認成功日時と確認済み契約版を更新しない。
- 更新候補は、有効スナップショットとして扱わない。
- 部分的に成功した外部確認を、確認成功として記録しない。
- 実行状態と更新候補は利用者別領域へ保存し、配布するSkill本体へ混在させない。
